{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "rm -rf venv",
        "path": "{{cwd}}"
      }
    },
    {
      "method": "notify",
      "params": {
        "html": "CSS Brand Assistant desinstalado. Los datos del usuario se conservan en la carpeta 'data'.",
        "type": "info"
      }
    }
  ]
}
